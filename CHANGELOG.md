# 变更记录

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的结构，并使用 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

暂无未发布变更。

## [1.1.0] - 2026-08-11

### Added

- 自主上传默认使用“自动识别（推荐）”，综合表头、值画像、重复关系和数据契约判断订单、仓库作业或物流轨迹数据；
- 新增快速导入摘要、一次批量采用推荐、一次安全忽略非必要字段，以及折叠的高级字段证据说明；
- 新增“一键整理并分析”：在一次操作内应用可信映射、生成安全内部 ID、忽略非分析列、重新校验并建立独立分析会话；
- 只有轨迹事件时也可计算首末轨迹平均/P50/P90、状态、承运商、地点、异常与时间线；OT/IF/OTIF 继续明确为不可计算；
- 分析上下文新增来源类型、输入指纹、关联率和能力矩阵；同一上下文贯穿总览、诊断、建议和报告；
- 扩充兼容性订单 CSV 至 80 行，兼容性 XLSX 的订单/仓库/轨迹工作表至 80/480/480 行，并同步内容指纹与目录元数据。

### Changed

- 统一日期解析器支持中英文月份、AM/PM、显式时区、日优先格式、整列 DMY/MDY 推断和 date-only 精度；
- 字段映射置信度分为 HIGH/MEDIUM/LOW：HIGH 自动应用，MEDIUM 一次批量确认，LOW 非必要列进入安全忽略；
- 普通导入不再以 21 列工程映射表作为首屏任务，内部字段代码降为高级说明。
- `tracking_events` 的最低可分析契约调整为运单、事件时间与原始状态；订单关联和承运商按各自能力启用，不再阻断基础轨迹分析。

### Fixed

- 修复重复语义的备注列在更强来源已满足标准字段后仍阻塞 XLSX 导入的问题；
- 修复部分真实英文日期与斜杠日期依赖系统 locale、纯日期被静默转成午夜时间的问题；
- 修复只有物流轨迹表时 P50/P90 等可支持分析仍为空白的问题；
- 浏览器端到端验收现在明确拒绝逐字段确认，并覆盖自动类型、批量映射、批量忽略、报告与建议。
- 修复用户导入轨迹后仍可能从旧 `localStorage` 读取兼容样例订单、使两份不同文件显示同一组指标的问题；激活新会话时会清理旧选择且切换文件重新计算；
- 修复总览、诊断和报告分别选择数据集导致上下文漂移的问题；三者现在暴露并校验同一个 `analysis_fingerprint`；
- 修复退回/取消轨迹被排除在首末时效外、未知状态被直接算作经营异常，以及只含运单号而无订单号的轨迹不能分组的问题；
- 当用户主动组合订单与事件且关联率为 0 时，系统拒绝静默混合并给出可操作提示。

### Acceptance

- 两份一次性 54×21 用户附件均完成 54/54 行浏览器导入、10 个运单/业务单/承运商、零丢行、零阻断和零逐字段确认；两份均无需逐字段或批量确认；
- A/B 轨迹首末跨度分别复算为 33.64/29.83/58.99 小时与 30.78/31.78/34.04 小时（Mean/P50/P90），输入指纹、时间范围、诊断和建议均不同；
- 这两份附件未提交仓库，不是生产、Cloudflare、CI 或 Release 依赖。通用能力继续由完全合成 fixture 验证。

## [1.0.0] - 2026-08-10

### Added

- 字段映射新增可撤销的“一键忽略可忽略项（N）”；只处理没有中等置信度候选、没有关键字段语义且不会影响最低数据契约的未处理列；
- 新增“一键应用推荐映射”，高置信度候选可批量采用，任何变更都会自动废弃旧质量报告并重新校验；
- 新增专业行动方案和管理层简报：两种视图共享同一组确定性事实、优先级、数据证据、KPI 和复验步骤；
- 新增可复现发布截图生成脚本和七张基于真实生产构建、固定合成数据的核心页面截图；
- 新增现实物流时间格式、混合中英文状态、异常标记和缺失轨迹事件 ID 的前后端回归测试。

### Changed

- 字段识别统一处理 Unicode NFKC、括号补充、全半角与常见分隔符，并从共享别名目录读取物流业务名称；有限值画像用于时间/数字候选和事件 ID 唯一性保护；
- 时间解析显式区分年优先、带 AM/PM 的月优先和连字符日优先格式；带偏移时间保留原偏移，无时区时间继续要求用户选择 IANA 时区；
- 物流状态标准化在保留 `raw_status` 的前提下，支持共享、可审查的中英文关键词与旧系统码辅助规则。
- Cloudflare 自主文件确认后由浏览器本地引擎继续生成指标、诊断、行动建议和报告；原始文件及标准化行仍不发送给 Worker。

### Fixed

- 防止重复批次号被相似度误映射为唯一 `tracking_event_id`；缺少可信事件 ID 时按标准语义字段与源行号生成稳定、唯一、可复现的 ID；
- `0`、`N`、`正常`、`无`、`-`、`常规` 不再被当作真实异常；只有布尔意义的 `1` 保留为 `GENERIC_EXCEPTION` 并产生透明警告；
- 修复 Windows 重启后 Wrangler 冷启动可能超过 20 秒、浏览器 E2E 因过短等待而误判失败的问题。
- 修复仓内诊断节点使用无序集合导致同权重发现和报告章节可能随 Python 哈希种子换序的问题；输出现在按业务流程稳定排列。
- 修复 Linux Chrome 下字段映射 E2E 可能在 Ant Design 弹层动画替换节点时读取到过期 0px 元素的问题；断言现在从同一 DOM 快照读取实际展开弹层，最低宽度标准保持不变。
- 修复浏览器本地分析派生订单数据集被误当作 IndexedDB 实体读取、导致自主轨迹导入后分析页白屏的问题；
- 修复已忽略列仍可能产生逐行必填错误的问题；缺失映射现在只产生一条包含原因、影响、建议操作的阻断项；
- 修复浏览器本地报告的 CSV 类型未按用途区分、订单导出仍指向远程 API，以及动态 HTML 标题未转义的问题；
- 修复行动建议辅助文本对比度不足导致生产可访问性审计失败的问题。

### Privacy and analysis boundaries

- 用户自主文件的原始内容和标准化行不发送给 Cloudflare Worker 或 Workers AI；建议引擎只消费确定性指标与诊断事实，AI 不可用时模板仍可工作；
- 一次性的 54×21 用户测试附件不是生产、部署、CI 或正式发布依赖。通用能力由公开的完全合成 fixture 回归验证。

### Known limitations

- Firefox/Safari 和物理移动设备尚未进入正式验收；
- PDF 中文字体、分页和长表未达到可靠导出门槛；
- Cloudflare 浏览器自有数据暂不支持 What-if，也不提供跨设备持久化或多用户权限。

## [1.0.0-rc.5] - 2026-08-09

### Added

- 字段映射新增显式“忽略”状态、取消忽略与自动/人工/忽略/待处理进度汇总；
- 浏览器与 FastAPI 质量报告新增 `ignored_source_columns` 和 `unresolved_source_columns`，保留用户决定与系统未知的语义差异；
- 新增忽略无关列、忽略必填候选、最终标准数据排除、桌面/移动端布局和真实 CSV/XLSX 流程回归。

### Changed

- 项目公开品牌从 FulfillLens CN 更名为 FulfillLens；用户界面、包元数据、文档、GitHub 仓库和 Cloudflare 正式入口改用不含 `CN` 的名称；
- 目标字段列扩展为主要工作区，桌面下拉菜单保持 480px 可读宽度，移动端改为全宽卡片布局；
- `Manual`、`Similarity`、`Ignored`、`Unresolved` 与置信度展示按真实映射状态重新对齐。

### Fixed

- 修复目标字段 Select 未设置可用宽度导致下拉内容逐字纵向挤压的问题；
- 修复“用户主动忽略”与“系统无法识别”都由空映射表示、进而产生状态冲突的问题；
- 防止忽略唯一必填源字段绕过标准 Schema：缺失目标必填字段仍是阻断错误；
- 被忽略列不进入标准行、指标、诊断、What-if、维度或报告数据集。
- 修复浏览器导入验收可能复用普通本地构建产物、误把文件发送到 Worker 的构建模式竞态；验收脚本现在先强制生成 Cloudflare 浏览器本地处理版本。

### Compatibility

- 为避免现有浏览器数据丢失，IndexedDB 内部数据库名暂保留 `fulfilllens-cn-browser-data`；Docker Compose 内部项目名也暂保留，二者不属于公开品牌；
- 历史 Release、CHANGELOG 和 Schema `$id` 中的旧名称/URI 仅作版本与契约兼容记录。

### Known limitations

- Cloudflare 浏览器自有数据仍未接入 Worker 指标、诊断、模拟和报告；完整分析继续使用本地/Docker 或公开合成案例路径；
- Firefox/Safari 与 PDF 仍未进入本候选版本发布验收。

## [1.0.0-rc.4] - 2026-08-09

### Added

- Cloudflare 在线版新增“自主上传文件”入口，沿用原七步导入向导并直接打开浏览器文件选择器；
- 新增浏览器本地 CSV/XLSX 安全解析、自动字段建议、时间/数量/状态标准化、Schema 与质量校验和 IndexedDB 确认存储；
- 新增共享机器可读字段目录，统一浏览器与 FastAPI 的中文/英文/camelCase/snake_case/业务系统别名；
- 新增非标准轨迹 CSV、多工作表 XLSX、360/390/430px、刷新持久化和错误文件的真实 Chrome 端到端验收。

### Changed

- Cloudflare 原始上传路由改为明确要求浏览器本地处理，不再用公开样例 SHA-256 白名单阻止合法自主文件；
- 导入预览补充文件、工作表、字段识别/未识别、自动匹配和人工确认统计；字段映射显示 Exact/Alias/Normalized/Similarity/Manual；
- 在线设置页可查看并删除当前浏览器内确认的数据集。

### Fixed

- 修复字段或状态映射变更后旧校验结果仍可能显示“可确认导入”的状态冲突，确认按钮现在只服从最新校验；
- 修复前端生产编译中的测试 Node 类型边界、IndexedDB 泛型和 `unknown` 值不安全字符串化；
- 浏览器安全上限与本地后端对齐：10 MiB、50,000 行、200 列、512 个 XLSX 条目和 50 MiB 解压上限。
- 修复 Linux CI 完成浏览器导入后只终止 `npx` 父进程、Wrangler 子进程仍持有输出流导致 job 不退出的问题；现按 Unix 进程组清理并保留强制兜底。

### Security

- Cloudflare 自主导入原始文件只在浏览器内存处理，确认/取消后释放引用；Worker、R2、D1 和日志不接收原始文件；
- XLSX 不执行宏、公式、脚本或外部链接；ZIP 路径、压缩比、展开规模与条目数在解压前后受限；
- 已确认的规范化数据只保存到当前浏览器 IndexedDB，并提供删除入口；Workers AI 不读取导入数据。

### Known limitations

- Cloudflare 浏览器自有数据尚未接入 Worker 指标、诊断、模拟和报告；完整分析仍使用本地/Docker 或公开合成案例路径；
- Firefox/Safari 和 PDF 仍未进入本轮发布验收。

## [1.0.0-rc.3] - 2026-08-09

### Added

- 增强中文、英文、camelCase、snake_case 和常见业务系统字段的自动匹配，并展示数据类型候选、匹配方式和置信度；
- 新增非标准订单 CSV 与多工作表物流 XLSX 两份完全合成兼容性样例；
- Cloudflare 在线演示可真实上传这两份摘要匹配样例，未知或真实文件继续明确拒绝。

### Fixed

- CSV 引号内换行不再被预拆行破坏，空白行会跳过并给出警告；
- XLSX 解析兼容缺失缓存行列维度的工作簿，并保留安全流式上限；
- Excel 连续零显示格式保留标识前导零；点号/中文日期和“妥投”状态可按明确规则转换；
- Docker API 与 Web 镜像加入兼容样例资产，避免容器目录和下载 404。

### Security

- 自动转换不补造数据、不修改业务事实；无法转换值进入质量报告；
- 公共 Worker 对允许上传文件执行 SHA-256 精确白名单校验，不持久化请求文件。

## [1.0.0-rc.2] - 2026-08-09

### Fixed

- 修复生产分块导致 Ant Design 初始化异常和真实浏览器白屏；
- 修复设置页无数据状态文字对比度不足；
- 修复 Docker API 镜像缺少合成案例资产，恢复一键载入；
- 补齐 Docker API 镜像的诊断规则运行期资产，恢复容器内透明诊断；
- 案例载入前置校验资产，并在数据集注册中途失败时回滚，避免孤儿数据。
- 修复 Cloudflare 在线演示 P50/P90 未使用 Type-7，以及 What-if 直接调整汇总指标的问题；在线模拟现从合成订单/节点副本变换后复算并校验参数。
- 修复在线报告跨 Worker 实例恢复时丢失筛选条件，以及合成节点时间线可能超出订单生命周期的问题。
- 修复在线有数据状态下指标、描述和成功标签对比度不足，以及动态表格缺少可聚焦滚动区、可读名称和分页按钮名称的问题；

### Changed

- GitHub Actions 官方 `checkout`、`setup-node`、`setup-python` 升级到 v7，消除 Node.js 20 运行时弃用警告；
- 浏览器审计在 Docker 中预载促销合成案例，覆盖真实有数据页面而非仅检查空状态；
- Docker smoke 增加真实 Chrome、360/768/1440 三视口和 axe/WCAG 审计；
- Cloudflare 从静态受限预览扩展为只处理公开合成案例的同源分析演示，仍拒绝真实文件和持久业务数据；
- 完成 Windows 重启后的本机 Docker、三案例、金标准、5 万订单性能和数据清理验收；
- 发布结论为可发布 `v1.0.0-rc.2` 预发布版本，正式 `v1.0.0` 仍需处理已知限制。

## [1.0.0-rc.1] - 2026-08-08

> 发布候选资料已准备，但尚未创建 GitHub Release、标签或远程发布。

### Added

- CSV/XLSX 七步导入、字段映射、质量报告、状态标准化和本地数据清理；
- OT、IF、OTIF、时效、节点停留、异常、取消、退回和覆盖率指标引擎；
- 趋势、分布、节点瓶颈、维度对比、订单证据和安全 CSV 导出；
- 八类透明诊断规则、证据追溯、严重度、帕累托和订单时间线；
- 四类订单/事件层 What-if 变换、敏感性分析和可复现指纹；
- 三套固定种子合成教学案例；
- Markdown、自包含 HTML 和安全 CSV 报告；
- 中文首次使用说明、移动端与可访问性加固；
- 中英文开源文档、社区治理模板、依赖许可与发布检查。

### Security

- 文件类型、MIME、解压炸弹、路径、公式注入、XSS、CORS、错误堆栈和敏感日志保护；
- 默认不导出敏感字段，Workers AI 默认关闭且不接触导入数据；
- API 安全响应头、非特权容器和受限 Compose 权限。
- 将构建链传递依赖 `brace-expansion` 与 `nanoid` 更新到修复已知拒绝服务问题的补丁版本。

### Performance

- Pandas 到 DuckDB 列式批量写入；
- 模拟按订单和事件建立索引，移除 O(n²) 扫描；
- 报告复用筛选结果并延迟读取订单样例。

### Known limitations

- Docker、Firefox、Safari 和 PDF 尚未完成正式发布验收；
- 当前后端不能不经迁移直接部署到 Cloudflare Workers；
- 诊断是透明规则判断，模拟是基于历史数据与简化假设的情景估算。
