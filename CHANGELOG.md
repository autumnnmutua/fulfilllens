# 变更记录

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的结构，并使用 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Planned

- 收集 RC 使用反馈并修复可复现回归；
- 评估 Firefox/Safari 兼容性和可复现 PDF 中文渲染；
- 评估 Cloudflare 上的完整后端迁移方案，同时保持本地优先和可解释数据边界。

## [1.0.0-rc.4] - 2026-08-09

### Added

- Cloudflare 在线版新增“自主上传文件”入口，沿用原七步导入向导并直接打开浏览器文件选择器；
- 新增浏览器本地 CSV/XLSX 安全解析、自动字段建议、时间/数量/状态标准化、Schema 与质量校验和 IndexedDB 确认存储；
- 新增共享机器可读字段目录，统一浏览器与 FastAPI 的中文/英文/camelCase/snake_case/业务系统别名；
- 新增非标准轨迹 CSV、多工作表 XLSX、360/390/430px、刷新持久化和错误文件的真实 Chrome 端到端验收。

### Changed

- Cloudflare 原始上传路由改为明确要求浏览器本地处理，不再用公开样例 SHA-256 白名单阻止合法自主文件；
- 导入预览补充文件、工作表、字段识别/未识别、自动匹配和人工确认统计；字段映射显示 Exact/Alias/Normalized/Similarity/Manual；
- 在线设置页可查看并删除当前浏览器内确认的数据集。

### Fixed

- 修复字段或状态映射变更后旧校验结果仍可能显示“可确认导入”的状态冲突，确认按钮现在只服从最新校验；
- 修复前端生产编译中的测试 Node 类型边界、IndexedDB 泛型和 `unknown` 值不安全字符串化；
- 浏览器安全上限与本地后端对齐：10 MiB、50,000 行、200 列、512 个 XLSX 条目和 50 MiB 解压上限。
- 修复 Linux CI 完成浏览器导入后只终止 `npx` 父进程、Wrangler 子进程仍持有输出流导致 job 不退出的问题；现按 Unix 进程组清理并保留强制兜底。

### Security

- Cloudflare 自主导入原始文件只在浏览器内存处理，确认/取消后释放引用；Worker、R2、D1 和日志不接收原始文件；
- XLSX 不执行宏、公式、脚本或外部链接；ZIP 路径、压缩比、展开规模与条目数在解压前后受限；
- 已确认的规范化数据只保存到当前浏览器 IndexedDB，并提供删除入口；Workers AI 不读取导入数据。

### Known limitations

- Cloudflare 浏览器自有数据尚未接入 Worker 指标、诊断、模拟和报告；完整分析仍使用本地/Docker 或公开合成案例路径；
- Firefox/Safari 和 PDF 仍未进入本轮发布验收。

## [1.0.0-rc.3] - 2026-08-09

### Added

- 增强中文、英文、camelCase、snake_case 和常见业务系统字段的自动匹配，并展示数据类型候选、匹配方式和置信度；
- 新增非标准订单 CSV 与多工作表物流 XLSX 两份完全合成兼容性样例；
- Cloudflare 在线演示可真实上传这两份摘要匹配样例，未知或真实文件继续明确拒绝。

### Fixed

- CSV 引号内换行不再被预拆行破坏，空白行会跳过并给出警告；
- XLSX 解析兼容缺失缓存行列维度的工作簿，并保留安全流式上限；
- Excel 连续零显示格式保留标识前导零；点号/中文日期和“妥投”状态可按明确规则转换；
- Docker API 与 Web 镜像加入兼容样例资产，避免容器目录和下载 404。

### Security

- 自动转换不补造数据、不修改业务事实；无法转换值进入质量报告；
- 公共 Worker 对允许上传文件执行 SHA-256 精确白名单校验，不持久化请求文件。

## [1.0.0-rc.2] - 2026-08-09

### Fixed

- 修复生产分块导致 Ant Design 初始化异常和真实浏览器白屏；
- 修复设置页无数据状态文字对比度不足；
- 修复 Docker API 镜像缺少合成案例资产，恢复一键载入；
- 补齐 Docker API 镜像的诊断规则运行期资产，恢复容器内透明诊断；
- 案例载入前置校验资产，并在数据集注册中途失败时回滚，避免孤儿数据。
- 修复 Cloudflare 在线演示 P50/P90 未使用 Type-7，以及 What-if 直接调整汇总指标的问题；在线模拟现从合成订单/节点副本变换后复算并校验参数。
- 修复在线报告跨 Worker 实例恢复时丢失筛选条件，以及合成节点时间线可能超出订单生命周期的问题。
- 修复在线有数据状态下指标、描述和成功标签对比度不足，以及动态表格缺少可聚焦滚动区、可读名称和分页按钮名称的问题；

### Changed

- GitHub Actions 官方 `checkout`、`setup-node`、`setup-python` 升级到 v7，消除 Node.js 20 运行时弃用警告；
- 浏览器审计在 Docker 中预载促销合成案例，覆盖真实有数据页面而非仅检查空状态；
- Docker smoke 增加真实 Chrome、360/768/1440 三视口和 axe/WCAG 审计；
- Cloudflare 从静态受限预览扩展为只处理公开合成案例的同源分析演示，仍拒绝真实文件和持久业务数据；
- 完成 Windows 重启后的本机 Docker、三案例、金标准、5 万订单性能和数据清理验收；
- 发布结论为可发布 `v1.0.0-rc.2` 预发布版本，正式 `v1.0.0` 仍需处理已知限制。

## [1.0.0-rc.1] - 2026-08-08

> 发布候选资料已准备，但尚未创建 GitHub Release、标签或远程发布。

### Added

- CSV/XLSX 七步导入、字段映射、质量报告、状态标准化和本地数据清理；
- OT、IF、OTIF、时效、节点停留、异常、取消、退回和覆盖率指标引擎；
- 趋势、分布、节点瓶颈、维度对比、订单证据和安全 CSV 导出；
- 八类透明诊断规则、证据追溯、严重度、帕累托和订单时间线；
- 四类订单/事件层 What-if 变换、敏感性分析和可复现指纹；
- 三套固定种子合成教学案例；
- Markdown、自包含 HTML 和安全 CSV 报告；
- 中文首次使用说明、移动端与可访问性加固；
- 中英文开源文档、社区治理模板、依赖许可与发布检查。

### Security

- 文件类型、MIME、解压炸弹、路径、公式注入、XSS、CORS、错误堆栈和敏感日志保护；
- 默认不导出敏感字段，Workers AI 默认关闭且不接触导入数据；
- API 安全响应头、非特权容器和受限 Compose 权限。
- 将构建链传递依赖 `brace-expansion` 与 `nanoid` 更新到修复已知拒绝服务问题的补丁版本。

### Performance

- Pandas 到 DuckDB 列式批量写入；
- 模拟按订单和事件建立索引，移除 O(n²) 扫描；
- 报告复用筛选结果并延迟读取订单样例。

### Known limitations

- Docker、Firefox、Safari 和 PDF 尚未完成正式发布验收；
- 当前后端不能不经迁移直接部署到 Cloudflare Workers；
- 诊断是透明规则判断，模拟是基于历史数据与简化假设的情景估算。
