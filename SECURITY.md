# 安全政策

## 支持范围

| 版本         | 状态                                       |
| ------------ | ------------------------------------------ |
| `main`       | 接收安全修复，但可能包含尚未发布的变化     |
| `1.0.0-rc.1` | 发布候选准备中；发布后在 RC 周期内接收修复 |
| 更早版本     | 未正式发布，不提供安全维护承诺             |

## 私密报告漏洞

远程仓库创建后，请启用 GitHub Private Vulnerability Reporting，并通过 **Security → Report a vulnerability** 提交。不要创建公开 Issue，也不要附真实订单、个人信息、有效 Token 或未经脱敏的日志。

报告建议包含：

- 受影响版本、模块和运行方式；
- 最小复现步骤和合成输入；
- 实际影响及可能的攻击前提；
- 已做的脱敏处理；
- 如有建议修复，请说明兼容性风险。

如果私密报告入口尚未启用，请通过仓库所有者 GitHub 资料中公开的私密联系方式联系。正式公开仓库前，维护者必须启用或公布一个有效的私密渠道。

## 响应目标

- 尽量在 3 个工作日内确认收到；
- 尽量在 10 个工作日内给出初步严重度和处理计划；
- 修复发布时间取决于影响、复现稳定性和回归风险，不承诺固定期限。

## 安全边界

- 文件导入限制扩展名、MIME、大小、XLSX 解压规模、行列数和单元格长度。
- CSV 导出对公式前缀安全转义；报告默认排除敏感字段。
- 日志和标准错误响应不得包含敏感原值、Token 或内部堆栈。
- Workers AI 默认关闭，只允许固定合成探针；浏览器不接收 Cloudflare Token。
- `.env`、数据库、上传文件、报告成品和本地分析数据均应保持未跟踪。
- 如果凭据曾出现在聊天、Issue、终端历史或日志中，即使 Git 扫描为零也应立即轮换。

## 不属于漏洞的内容

- 透明规则的合理误报或业务阈值分歧，请使用 Feature/Data Mapping Issue；
- 模拟结果与真实经营效果不同属于已声明的模型限制，除非存在数据泄露、越权或结果篡改；
- 仅影响已停止支持版本且无法在当前版本复现的问题。

安全设计与已知限制见 [风险登记](docs/RISKS.md)、[导入规范](docs/IMPORTING.md)和[阶段 10 审查](docs/QA_STAGE10.md)。
